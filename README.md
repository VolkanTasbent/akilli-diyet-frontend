# Akıllı Diyet — Frontend

React 19, Vite, TypeScript. API istekleri geliştirmede Vite proxy ile `http://localhost:8080` adresine yönlendirilir.

## Çalıştırma

```bash
npm install
npm run dev
```

Backend’in 8080 portunda çalışıyor olması gerekir.
